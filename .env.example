# Database Configuration
# Required: Database connection string for Prisma ORM
# For SQLite (development): file:./database.sqlite
# For PostgreSQL: postgresql://user:password@localhost:5432/formly
DATABASE_URL=file:./database.sqlite

# Authentication & JWT Configuration
# JWKS endpoint for JWT token verification
# Default for local development with PlaceNet API
JWKS_ENDPOINT=http://localhost:3003/.well-known/jwks.json

# Hashing & Security
# Salt for generating hashids (obfuscated form IDs in URLs)
# Change this to a random string in production
HASHID_SALT=formly-salt

# Server Configuration (optional)
# Node environment (development, production, test)
NODE_ENV=development

# Maximum request body size (useful for large form submissions)
BODY_SIZE_LIMIT=10M

# Server port (defaults to 3000 in production, 3007 in dev)
PORT=3007
